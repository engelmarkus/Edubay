<%= form_for(@doc_type) do |f| %>
  <% if @doc_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doc_type.errors.count, "error") %> prohibited this Document Type from being saved:</h2>

      <ul>
      <% @doc_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
